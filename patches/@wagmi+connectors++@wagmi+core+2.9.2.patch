diff --git a/node_modules/@wagmi/connectors/node_modules/@wagmi/core/dist/esm/connectors/injected.js b/node_modules/@wagmi/connectors/node_modules/@wagmi/core/dist/esm/connectors/injected.js
index 4d62a5b..3660d9e 100644
--- a/node_modules/@wagmi/connectors/node_modules/@wagmi/core/dist/esm/connectors/injected.js
+++ b/node_modules/@wagmi/connectors/node_modules/@wagmi/core/dist/esm/connectors/injected.js
@@ -154,7 +154,7 @@ export function injected(parameters = {}) {
             try {
                 if (!accounts?.length && !isReconnecting) {
                     const requestedAccounts = await provider.request({
-                        method: 'eth_requestAccounts',
+                        method: 'quai_requestAccounts',
                     });
                     accounts = requestedAccounts.map((x) => getAddress(x));
                 }
@@ -231,14 +231,14 @@ export function injected(parameters = {}) {
             const provider = await this.getProvider();
             if (!provider)
                 throw new ProviderNotFoundError();
-            const accounts = await provider.request({ method: 'eth_accounts' });
+            const accounts = await provider.request({ method: 'quai_accounts' });
             return accounts.map((x) => getAddress(x));
         },
         async getChainId() {
             const provider = await this.getProvider();
             if (!provider)
                 throw new ProviderNotFoundError();
-            const hexChainId = await provider.request({ method: 'eth_chainId' });
+            const hexChainId = await provider.request({ method: 'quai_chainId' });
             return Number(hexChainId);
         },
         async getProvider() {
